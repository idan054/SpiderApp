# Automatically generated on 2020-10-10 UTC from https://codemagic.io/app/5f81f8bada606005f992a7f1/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:
  default-workflow:
    name: Default Workflow
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - cd . && flutter packages pub get
      - cd . && flutter analyze
      - cd . && flutter test
    artifacts:
      - flutter_drive.log
    publishing:
      email:
        recipients:
          - idanbit80@gmail.com
